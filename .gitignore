# Lake build artifacts
/.lake/
/build/

# Nix / direnv
/.direnv/
/result
/result-*

# Machine-local direnv shim (the tracked config is .envrc.shared)
/.envrc

# Generated by git-hooks.nix on dev-shell entry (contains machine-local store paths)
/.pre-commit-config.yaml
